# Configuração do banco de dados
# IMPORTANTE: Quando usar docker-compose, não precisa configurar este arquivo
# As variáveis já estão configuradas no docker-compose.yml

# Para usar banco local do Docker Compose (padrão):
DATABASE_URL="postgresql://admin:senha123@db:5432/taxas_turismo?schema=public"

# Path portal padrão da prefeitura
PATH_PORTAL="ilha-grande-pi"